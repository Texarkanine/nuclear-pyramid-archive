# Task: Encoding Fix + docs/site Restructure

* Task ID: encoding-restructure
* Complexity: Level 2
* Type: Bug fix + simple enhancement

Fix charset mojibake (UTF-8 content declared as iso-8859-1) and restructure output directory so archive content lives under `docs/site/` with a redirect `docs/index.html` at the root.

## Test Plan (TDD)

### Behaviors to Verify

- **Charset rewrite (iso-8859-1)**: HTML with `charset=iso-8859-1` → output has `charset=utf-8`
- **Charset rewrite (windows-1252)**: HTML with `charset=windows-1252` → output has `charset=utf-8`
- **Charset no-op (utf-8)**: HTML already declaring `charset=utf-8` → unchanged
- **Charset no-op (no declaration)**: HTML with no charset → unchanged
- **Build applies charset rewrite**: `Transform.build` output files have `charset=utf-8` when source had `charset=iso-8859-1`
- **Scaffolding creates redirect**: `write_scaffolding(dir)` creates `dir/index.html` redirecting to `site/index.php`
- **Scaffolding creates .nojekyll**: `write_scaffolding(dir)` creates `dir/.nojekyll`
- **Build does NOT create .nojekyll**: `Transform.build(...)` no longer creates `.nojekyll` in dest_dir
- **Edge: existing .nojekyll test removed/updated**: No false assertion about `.nojekyll` in build output

### Test Infrastructure

- Framework: Minitest (autorun)
- Test location: `test/`
- Conventions: one `*_test.rb` file per module, class-per-concern with descriptive method names
- New test files: none (add to `test/transform_test.rb`)

## Implementation Plan

### Step 1: Add `rewrite_charset` method + tests

- Files: `test/transform_test.rb`, `lib/transform.rb`
- Tests: Add `CharsetRewriteTest` class with 4 test cases (iso-8859-1, windows-1252, utf-8 no-op, no-declaration no-op)
- Code: Add `self.rewrite_charset(html)` — gsub `charset=(iso-8859-1|windows-1252)` → `charset=utf-8`

### Step 2: Wire charset rewrite into build pipeline + test

- Files: `test/transform_test.rb`, `lib/transform.rb`
- Tests: Add `test_build_rewrites_charset_in_html` to `BuildIntegrationTest`
- Code: Call `rewrite_charset(content)` in `build` method after `inject_about_nav`

### Step 3: Extract `.nojekyll` from build into `write_scaffolding` + tests

- Files: `test/transform_test.rb`, `lib/transform.rb`
- Tests: Add `ScaffoldingTest` class with `test_creates_nojekyll` and `test_creates_redirect`; change `test_build_creates_nojekyll` to `test_build_does_not_create_nojekyll`
- Code: Add `self.write_scaffolding(docs_dir)` that creates `.nojekyll` and redirect `index.html`. Remove `.nojekyll` from `build`.

### Step 4: Update Rakefile for docs/site structure

- Files: `Rakefile`
- Changes: Rename `DEST_DIR` → `DOCS_DIR`; add `SITE_DIR = docs/site`; update `transform` task to build into `SITE_DIR` and call `write_scaffolding(DOCS_DIR)`; update `clean` task to clean `DOCS_DIR`

### Step 5: Update src/index.html redirect path

- Files: `src/index.html`
- Changes: Update redirect from `index.php` to `site/index.php`

### Step 6: Update README and documentation

- Files: `README.md`
- Changes: Update directory structure description, file manifest note about `docs/site/`

## Technology Validation

No new technology — validation not required.

## Dependencies

- None new. Existing: `minitest`, `rake`, `fileutils`

## Challenges & Mitigations

- **Test fixture encoding**: The test fixture `index.php` may not have actual Windows-1252 smart quotes. Mitigation: the charset rewrite test can use a simple string containing the charset declaration; the build integration test verifies the charset tag is rewritten.
- **src/index.html dual-purpose**: `src/index.html` gets copied by `copy_src_files` to `docs/site/index.html` (relative redirect to `index.php`). The docs-root `docs/index.html` is generated by `write_scaffolding` (redirects to `site/index.php`). Both are correct for their respective locations.

## Status

- [x] Initialization complete
- [x] Test planning complete (TDD)
- [x] Implementation plan complete
- [x] Technology validation complete
- [ ] Preflight
- [ ] Build
- [ ] QA
